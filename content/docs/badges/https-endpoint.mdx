---
title: HTTPS Endpoint
description: Create a badge from any HTTPS endpoint that returns JSON.
badge: "/https/cal-badge-icd0onfvrxx6.runkit.sh.svg"
---


<BadgeSandbox
  endpoint="/https/:url.svg"
  pathParams={[
    { name: "url", label: "endpoint URL (without https://)", placeholder: "cal-badge-icd0onfvrxx6.runkit.sh", required: true, description: "Any HTTPS URL that returns JSON with label/value/color fields." },
  ]}
  defaults={{ url: "cal-badge-icd0onfvrxx6.runkit.sh" }}
/>

## How it works

Create an HTTPS endpoint on any platform (RunKit, Vercel, Cloudflare Workers, etc.) that returns JSON:

```json
{
  "label": "hello",
  "value": "world",
  "color": "blue"
}
```

The badgen format is also supported:

```json
{
  "subject": "hello",
  "status": "world",
  "color": "blue"
}
```

Then use it as a badge:

<CodeLine language="markdown" code="![badge](https://shieldcn.dev/https/your-endpoint.example.com/path/args)" />

## Path arguments

Any path segments after the hostname are forwarded to the endpoint:

| Badge URL | Fetches |
|-----------|---------|
| `/https/example.com` | `https://example.com` |
| `/https/example.com/foo/bar` | `https://example.com/foo/bar` |

## Response format

The endpoint must return JSON with these fields (all optional except `value`/`status`):

<ApiRefTable
  title="Endpoint response"
  props={[
    { name: "label", type: "string", default: '"badge"', description: "Left-side text. Also accepts 'subject' (badgen compat)." },
    { name: "value", type: "string", description: "Right-side text. Also accepts 'status' or 'message'.", required: true },
    { name: "color", type: "string", description: "Badge color (hex or named color)." },
  ]}
/>

## Examples

<BadgePreviewGroup>
  <BadgePreviewCard src="/https/cal-badge-icd0onfvrxx6.runkit.sh.svg" alt="endpoint" description="Default variant" />
  <BadgePreviewCard src="/https/cal-badge-icd0onfvrxx6.runkit.sh.svg?variant=secondary" alt="endpoint" description="Secondary variant" />
  <BadgePreviewCard src="/https/cal-badge-icd0onfvrxx6.runkit.sh.svg?variant=outline" alt="endpoint" description="Outline variant" />
  <BadgePreviewCard src="/https/cal-badge-icd0onfvrxx6.runkit.sh.svg?variant=branded" alt="endpoint" description="Branded variant" />
</BadgePreviewGroup>

## Copy-paste examples

<BadgePreview src="/https/cal-badge-icd0onfvrxx6.runkit.sh.svg" alt="https endpoint" description="Default — pull badge data from any HTTPS endpoint" />

<BadgePreview src="/https/cal-badge-icd0onfvrxx6.runkit.sh.svg?variant=secondary" alt="https endpoint" description="Secondary — softer look for README rows" />

## Caching

Endpoint responses are cached for 5 minutes.

## Credits

This endpoint format is inspired by [badgen.net](https://badgen.net)'s HTTPS endpoint feature.
